version: '3.8'

services:
  streamxr:
    build: .
    container_name: streamxr
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./public/models:/app/public/models:ro
